'use strict';

describe('Service: firefly', function () {

  // load the service's module
  beforeEach(module('apparatusApp'));

  // instantiate service
  var firefly, angularFire;
  beforeEach(inject(function (_firefly_) {
    firefly = _firefly_;
  }));

  // it('should do something', function () {
  // });
});
